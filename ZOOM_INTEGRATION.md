# 🎥 Интеграция Wav2Lip с Zoom

## Концепция

Вы говорите в микрофон → Ваш аватар двигает губами синхронно → Zoom транслирует аватар участникам встречи

## Архитектура

```
┌─────────────┐
│  Микрофон   │  ← Ваш голос
└──────┬──────┘
       │
       ▼
┌─────────────────────────┐
│  PyAudio                │  Захват аудио
│  (realtime audio)       │
└──────┬──────────────────┘
       │
       ▼
┌─────────────────────────┐
│  Wav2Lip + OpenVINO     │  Синхронизация губ
│  (AI lip-sync)          │
└──────┬──────────────────┘
       │
       ▼
┌─────────────────────────┐
│  pyvirtualcam           │  Виртуальная камера
│  (Virtual Camera)       │
└──────┬──────────────────┘
       │
       ▼
┌─────────────────────────┐
│  Zoom                   │  Видеозвонок
│  (участники видят       │
│   вашего аватара)       │
└─────────────────────────┘
```

## Установка

### Шаг 1: Установить pyvirtualcam

```bash
pip3 install pyvirtualcam
```

### Шаг 2: Установить OBS Virtual Camera (для macOS)

**Вариант A: OBS Studio (Рекомендуется)**

1. Скачайте OBS Studio:
   ```bash
   # Если есть Homebrew (без sudo):
   brew install --cask obs

   # Или скачайте вручную:
   # https://obsproject.com/download
   ```

2. Запустите OBS один раз для установки виртуальной камеры
3. Закройте OBS - виртуальная камера останется доступной

**Вариант B: OBS Virtual Camera Plugin**

Скачайте напрямую:
https://github.com/johnboiles/obs-mac-virtualcam/releases

### Шаг 3: Проверка установки

```bash
python3 -c "import pyvirtualcam; print('✅ pyvirtualcam установлен')"
```

## Использование

### 1. Подготовьте изображение аватара

Используйте фото вашего лица (или любого другого):
- Формат: JPG, PNG
- Рекомендуемое разрешение: 1280x720 или выше
- Лицо должно быть видно четко
- Сохраните в `assets/uploaded_images/avatar.jpg`

### 2. Запустите виртуальную камеру

```bash
python3 virtual_camera.py assets/uploaded_images/avatar.jpg
```

Вы увидите:
```
============================================================
  WAV2LIP VIRTUAL CAMERA FOR ZOOM
============================================================
Загрузка модели Wav2Lip...
Виртуальная камера готова!

Запуск виртуальной камеры 1280x720...
Zoom увидит камеру как 'OBS Virtual Camera' или 'pyvirtualcam'

Инструкции:
1. Откройте Zoom
2. Settings → Video → Camera
3. Выберите 'OBS Virtual Camera' или 'pyvirtualcam'
4. Говорите в микрофон - аватар будет двигать губами!

Нажмите Ctrl+C для остановки

✅ Виртуальная камера создана: OBS Virtual Camera
   Разрешение: 1280x720
   FPS: 30

🎤 Говорите в микрофон...
```

### 3. Настройте Zoom

1. **Откройте Zoom**

2. **Зайдите в настройки:**
   - Нажмите на значок шестеренки (Settings)
   - Или: zoom.us → Settings

3. **Выберите виртуальную камеру:**
   - Перейдите в раздел **Video**
   - В выпадающем списке **Camera** выберите:
     - **OBS Virtual Camera** (если установлен OBS)
     - или **pyvirtualcam**

4. **Проверьте:**
   - В превью должен отобразиться ваш аватар
   - Говорите - губы должны двигаться!

5. **Присоединитесь к звонку:**
   - Включите видео
   - Участники увидят ваш аватар с синхронизацией губ

### 4. Важные настройки Zoom

**Отключить виртуальный фон:**
- Settings → Background & Effects → None
- Иначе Zoom может конфликтовать с виртуальной камерой

**Включить HD видео:**
- Settings → Video → Enable HD (если хотите 720p)

**Микрофон:**
- Используйте ваш обычный микрофон
- Убедитесь что он НЕ заглушен (unmute)

## Использование в реальном звонке

### Сценарий 1: Презентация для банка

1. Запустите `python3 virtual_camera.py avatar.jpg` **до** начала встречи
2. Откройте Zoom, выберите виртуальную камеру
3. Присоединитесь к встречи
4. Говорите как обычно - аватар будет двигать губами
5. Участники увидят профессиональный аватар вместо вашего живого видео

### Сценарий 2: Анонимное участие

- Используйте нейтральное лицо или стилизованный аватар
- Ваш голос остается, но лицо защищено

### Сценарий 3: Брендированный аватар

- Используйте логотип компании с лицом
- Или корпоративного персонажа

## Производительность

**Минимальные требования:**
- CPU: Intel i5 / Apple M1 или выше
- RAM: 4 GB
- Микрофон: любой USB или встроенный

**Ожидаемые показатели:**
- Задержка: ~1-1.5 сек (приемлемо)
- FPS: 25-30 (плавное видео)
- CPU: 30-50% одного ядра

**Оптимизация для банковской презентации:**

Если нужна меньшая задержка:
1. Уменьшите CHUNK в `virtual_camera.py`:
   ```python
   self.CHUNK = 4000  # 0.25 сек вместо 0.5
   ```
2. Используйте более быстрый компьютер

## Troubleshooting

### Проблема 1: Zoom не видит виртуальную камеру

**Решение:**
```bash
# 1. Проверьте что OBS Virtual Camera установлен:
ls /Library/CoreMediaIO/Plug-Ins/DAL/

# Должен быть: obs-mac-virtualcam.plugin

# 2. Перезапустите Zoom полностью (Quit, не просто закрыть окно)

# 3. Попробуйте запустить OBS один раз и закрыть
```

### Проблема 2: Черный экран в Zoom

**Решение:**
- Убедитесь что `virtual_camera.py` запущен и работает
- Проверьте права доступа к камере: System Preferences → Security & Privacy → Camera
- Добавьте Python и Zoom в разрешенные приложения

### Проблема 3: Губы не двигаются

**Решение:**
```bash
# Проверьте что микрофон работает:
python3 -c "import pyaudio; p=pyaudio.PyAudio(); print(p.get_device_count())"

# Должно быть > 0 устройств
```

### Проблема 4: Большая задержка (>2 сек)

**Решение:**
- Закройте другие тяжелые приложения
- Уменьшите CHUNK для меньшей задержки
- Убедитесь что используется OpenVINO (CPU оптимизация)

### Проблема 5: Zoom просит разрешения на камеру

**Решение:**
```bash
# Для macOS 10.14+:
# System Preferences → Security & Privacy → Camera
# Добавьте галочку для Zoom и Python
```

## Альтернативные решения

### Вариант 1: OBS Studio напрямую

Если `pyvirtualcam` не работает:

1. Откройте OBS Studio
2. Добавьте источник: Window Capture
3. Выберите окно с вашим браузером (где открыт http://127.0.0.1:8080)
4. Start Virtual Camera в OBS
5. Zoom выберет OBS Virtual Camera

### Вариант 2: ManyCam (коммерческое решение)

- Скачайте ManyCam: https://manycam.com
- Добавьте источник: Desktop/Window
- Выберите окно с Wav2Lip
- В Zoom выберите ManyCam Virtual Webcam

### Вариант 3: CamTwist (бесплатный для macOS)

- Скачайте CamTwist: http://camtwiststudio.com
- Setup: Desktop → Select window
- В Zoom выберите CamTwist

## Продвинутые настройки

### Изменить разрешение

В `virtual_camera.py` измените:
```python
self.output_width = 1920  # Full HD
self.output_height = 1080
```

### Изменить FPS

```python
with pyvirtualcam.Camera(..., fps=60) as cam:  # 60 FPS
```

### Использовать несколько аватаров

Создайте скрипт переключения:
```bash
# Аватар 1 (формальный)
python3 virtual_camera.py formal_avatar.jpg

# Аватар 2 (casual)
python3 virtual_camera.py casual_avatar.jpg
```

## Безопасность

**Важно для банковской презентации:**

1. **Приватность:**
   - Виртуальная камера работает локально
   - Нет отправки данных в интернет (кроме Zoom)
   - Аудио остается вашим оригинальным голосом

2. **Прозрачность:**
   - Сообщите участникам что используете аватар
   - Это показывает технологическую компетентность

3. **Резервный план:**
   - Держите реальную камеру готовой
   - Если что-то пойдет не так, переключитесь на обычное видео

## Чек-лист перед важной встречей

- [ ] Протестировали виртуальную камеру заранее
- [ ] Проверили синхронизацию губ
- [ ] Убедились что микрофон работает
- [ ] Закрыли другие тяжелые приложения
- [ ] Проверили качество интернета
- [ ] Zoom обновлен до последней версии
- [ ] Имеете план Б (реальная камера)

## Команды для быстрого старта

```bash
# 1. Загрузите фото аватара
cp /path/to/your/photo.jpg assets/uploaded_images/avatar.jpg

# 2. Запустите виртуальную камеру
python3 virtual_camera.py assets/uploaded_images/avatar.jpg

# 3. Откройте Zoom → Settings → Video → выберите OBS Virtual Camera

# 4. Присоединитесь к звонку и говорите!
```

## Часто задаваемые вопросы

**Q: Можно ли использовать GPU для ускорения?**
A: На macOS используется CPU через OpenVINO, это уже оптимизировано.

**Q: Работает ли с Google Meet / Microsoft Teams?**
A: Да! Любое приложение, которое поддерживает виртуальные камеры.

**Q: Могут ли участники понять что это не я?**
A: Если синхронизация хорошая, сложно заметить. Но лучше быть честным.

**Q: Сколько CPU использует?**
A: Около 30-50% одного ядра на Intel i5/i7 или Apple M1.

**Q: Можно ли использовать анимированного персонажа?**
A: Да, Wav2Lip работает с любыми лицами, даже мультяшными.

## Поддержка

Если возникли проблемы:

1. Проверьте `server.log` - там есть подробности ошибок
2. Убедитесь что все зависимости установлены: `pip3 list`
3. Проверьте что OBS Virtual Camera установлен
4. Перезапустите Zoom

---

**Создано:** 2025-11-14
**Версия:** 1.0
**Совместимость:** macOS 10.14+, Zoom 5.0+
