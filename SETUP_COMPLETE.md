# Wav2Lip Realtime - Готово к работе! ✓

## Статус установки

✅ **Python 3.9.6** - установлен
✅ **Зависимости** - установлены (Flask, OpenVINO, OpenCV, Torch и др.)
✅ **Модели** - скачаны и размещены
✅ **Flask сервер** - запущен и работает

## Скачанные модели

### Face Detection Model
- **Файл:** `checkpoints/mobilenet.pth` (1.7 MB)
- **Источник:** Hugging Face (py-feat/retinaface)
- **Назначение:** Детекция лиц на изображениях

### OpenVINO Wav2Lip Model
- **Файлы:**
  - `openvino_model/wav2lip_openvino_model.xml` (434 KB)
  - `openvino_model/wav2lip_openvino_model.bin` (69 MB)
  - `openvino_model/wav2lip_onnx_export.onnx` (138 MB)
- **Назначение:** Оптимизированная модель для синхронизации губ

## Доступ к приложению

🌐 **Веб-интерфейс доступен по адресам:**
- http://127.0.0.1:8080
- http://172.18.98.101:8080

## Как использовать

1. **Откройте браузер** и перейдите по адресу http://127.0.0.1:8080

2. **Загрузите изображение:**
   - Нажмите кнопку выбора файла
   - Выберите изображение с лицом (jpg, png)
   - Изображение появится в интерфейсе

3. **Запустите синхронизацию:**
   - Нажмите кнопку "Start"
   - Приложение будет захватывать аудио с микрофона*
   - Губы на изображении будут синхронизироваться с речью

4. **Остановите:**
   - Нажмите "Stop" для паузы
   - Нажмите "Clear" для загрузки нового изображения

## Важные замечания

### PyAudio (Захват аудио с микрофона)
⚠️ **Статус:** Используется временная заглушка

Для полной функциональности захвата аудио с микрофона необходимо:

1. Установить Homebrew (требует sudo):
   ```bash
   /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
   ```

2. Установить PortAudio:
   ```bash
   brew install portaudio
   ```

3. Удалить заглушку и установить PyAudio:
   ```bash
   rm pyaudio.py
   pip3 install pyaudio
   ```

4. Перезапустить приложение:
   ```bash
   pkill -f "python3 app.py"
   python3 app.py
   ```

**Текущее поведение без PyAudio:**
- Приложение работает без реального захвата аудио
- Используется "тишина" вместо микрофона
- Основная функциональность lip-sync доступна с предзаписанным аудио

## Технические детали

### Оптимизация
- **OpenVINO 2025.3.0** - для ускорения инференса на CPU
- **Производительность:** ~0.26 сек на кадр (оптимизированная версия)

### Архитектура
- **Backend:** Flask (Python)
- **Deep Learning:** PyTorch + OpenVINO
- **Face Detection:** RetinaFace (MobileNet 0.25)
- **Lip-Sync:** Wav2Lip GAN

### Структура проекта
```
realtimeWav2lip-main/
├── app.py                      # Flask приложение
├── inference.py                # Логика инференса
├── checkpoints/
│   └── mobilenet.pth          # Модель детекции лиц
├── openvino_model/
│   ├── wav2lip_openvino_model.xml
│   ├── wav2lip_openvino_model.bin
│   └── wav2lip_onnx_export.onnx
├── assets/
│   └── uploaded_images/       # Загруженные пользователем изображения
├── static/                     # CSS, JS, изображения для UI
└── templates/                  # HTML шаблоны

```

## Управление приложением

### Остановить сервер:
```bash
pkill -f "python3 app.py"
```

### Запустить сервер:
```bash
python3 app.py
```

### Проверить статус:
```bash
lsof -ti:8080
```

## Дополнительная информация

- **Оригинальный репозиторий:** https://github.com/Rudrabha/Wav2Lip
- **Научная статья:** http://cdn.iiit.ac.in/cdn/cvit.iiit.ac.in/images/Projects/Speech-to-Lip/paper.pdf

---

**Дата установки:** 2025-11-13
**Версия Python:** 3.9.6
**Версия OpenVINO:** 2025.3.0
